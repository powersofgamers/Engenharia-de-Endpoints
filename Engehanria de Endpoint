
Passos para Analisar ENDPOINT

# Simulando um iPhone (iOS)
curl -I -L -A "Mozilla/5.0 (iPhone; CPU iPhone OS 16_5 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/16.5 Mobile/15E148 Safari/604.1" (coloca o link do endpoint aqui)

# Simulando um dispositivo Android
curl -I -L -A "Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Mobile Safari/537.36" (Coloca o link do endpoint aqui)

Analise da pagina do Endpoint
curl -A "Mozilla/5.0" -o analise_page.html ( Coloque o link)

Busca de links suspeitos com grep
grep -Eio '(http|https)://[^"]+' analise_page.html

(Roteamento de Rota)

http -F --follow (link)

Verificar Ips e APontamentos DNS
dig (link) ANY

Verificar a Propriedade de dominio 
 whois (link)


